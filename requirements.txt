# ── Metal Price API v3 — httpx + BeautifulSoup Architecture ──
# No more headless browser — pure HTTP + SSR JSON extraction

# HTTP client — async, connection pooling, HTTP/2
httpx[http2]==0.28.1

# HTML parsing — SSR JSON extraction from <script id="__NEXT_DATA__">
beautifulsoup4==4.13.4
lxml==5.3.2

# API framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Redis — hiredis C-extension for maximum throughput
redis[hiredis]==5.2.1

# Data validation
pydantic==2.10.3